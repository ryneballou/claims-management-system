```markdown
# CLAUDE.md — Claims Management System

## Project Overview
SaaS claims management platform for insurance carriers and TPAs. Multi-tenant,
configurable workflow engine, AI-powered triage and fraud detection, full API platform.

## Tech Stack
<!-- Update this after Chat 1 (Architecture & Tech Stack) -->
- Frontend: TBD
- Backend: TBD
- Database: TBD
- Cloud: TBD
- Message Broker: TBD

## Project Structure
<!-- Update as the codebase grows -->
- `/docs` — Architecture decisions, schemas, integration specs
- `/src` — Application source code
- `/tests` — Test suite
- `/infra` — Terraform/IaC configs

## Development Conventions
- All architecture decisions are recorded in `/docs/architecture/` as ADR files
- Changelog entries follow the format in `CHANGELOG.md`
- Commits should reference the Claude Chat # that produced them (e.g., "Chat 5: Add workflow engine state machine")
- Code must include error handling, input validation, and JSDoc/docstring comments

## Key Commands
<!-- Update once your build tooling is set up -->
- `npm run dev` — Start development server
- `npm run test` — Run test suite
- `npm run build` — Production build
- `npm run migrate` — Run database migrations

## Important Context
- See `docs/features.md` for the full feature list
- See `docs/chat-roadmap.md` for the development plan and chat breakdown
- See `CHANGELOG.md` for all changes and the Claude Chat Index
```
